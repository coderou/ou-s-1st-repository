>>>>>>>>>复习
  1.1px的边框问题
    问题的原因:1px的边框在高清屏上展示的效果看起来比较粗,不是恨美观
    希望得到的效果:希望1px的边框在高清屏上,看起来是一个比较细的效果
    解决办法:
      1.媒体查询,如果是像素比为2,那么边框的宽度是0.5px,像素比为3,边框的宽度是0.3333px
      2.通过viewport 将布局视口 变大,但是盒子的px值不变,这样盒子看起来就变小了,此时边框看起来也变小了,然后将根标签的字体大小变大,那么盒子也就跟着变大了(其他的盒子正常适配,但是边框不适配)
  2.移动端事件
    touchstart 手指触摸屏幕
    touchmove 手指移动
    touchend 手指离开屏幕
    touchcancel 中断触摸
    事件对象身上的属性:
    touches 屏幕上的所有触点
    targetTouches 元素上的所有触点
    changedTouches 改变的触点
    如何获取离开屏幕的触点?
    在touchend事件中,通过changedTouches获取
    注意:
      1.touch事件只在移动端生效
      2.移动端也支持click事件,但是比touchstart要延迟约300ms(如果开启了理想视口,约100ms)
      3.会出现点击穿透(当触发了touchstart事件,约300ms后,同样的位置会触发click事件)
        解决:在touchstart的事件处理函数中执行e.preventDefault()
      
>>>>>>>>>笔记
  1.babel-plugin-import:按需加载
  2.postcss-px2rem:px转换rem
  3.InputItem 的clear,会在输入后有清楚所有内容的按钮,但是会导致布局变动
  4.适配方式1和2是不一样的,同时写法也不一样
  5.Link和history.push都可以记录历史记录,
    push的好处是点返回可以直接返回上一级页面,
    但是不好的地方是页面复杂的时候返回可能不正常,
    replace跳转的时候可以传递第二个参数(从哪里跳转过来的)
    所以页面复杂的时候可以选择replace
  6.getFieldProps()返回的是对象,jsx中可以...结构
  7.装全局包建议用npm装!!!如果用yarn要在vsc里面配置
  8.同源策略是浏览器的安全策略,服务器和服务器是没有同源策略的
    这时候在本地开发服务器开一个中介,发请求到代理身上,代理发请求给后台服务器,代理在响应到浏览器,就解决了跨域问题
  9.toast:吐司框:出现一小会儿,然后又消失了,最早是移动端才有的
  10.开发中,发送验证码要先注释掉,因为都是要花钱的
  11.改变视图就定义state动态控制状态

>>>>>>>>>其他
  1.已更改formatter配置,单引号,行尾不自动添加分号,
  2.虽然在组件问价链家下面下index.js很方便,但是也仅仅是引入的时候方便,在文件件使用ctrl+e的方式切换,根本找不到那个index,草
  10.如果你觉得你学的可以了,请闭上眼睛,再回答你学的怎么样

>>>>>>>>>